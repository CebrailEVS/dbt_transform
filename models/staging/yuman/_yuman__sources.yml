# models/_yuman_api__sources.yml
version: 2

sources:
  - name: yuman_api
    schema: raw
    description: "Données brutes extraites de l'API Yuman"  
    tables:
      - name: yuman_clients
        description: "Table des clients Yuman"  
        columns:
          - name: id
            tests:
              - unique
              - not_null
            description: "Identifiant unique du client"

      - name: yuman_sites
        description: "Table des sites Yuman"
        columns:
          - name: id
            tests:
              - unique 
              - not_null
            description: "Identifiant unique du site"
          - name: client_id
            tests:
              - not_null
            description: "Référence vers le client propriétaire"  

      - name: yuman_materials
        description: "Table des machines Yuman"
        columns:
          - name: id
            tests:
              - unique 
              - not_null
            description: "Identifiant unique de la machine"
          - name: site_id
            tests:
              - not_null
            description: "Référence vers le site propriétaire"

      - name: yuman_material_categories
        description: "Table des catégories de machines Yuman pour mapper les machines aux catégories"
        columns:
          - name: id
            tests:
              - unique 
              - not_null
            description: "Identifiant unique de la catégorie machine"

      - name: yuman_products
        description: "Table des produits Yuman"
        columns:
          - name: id
            tests:
              - unique 
              - not_null
            description: "Identifiant unique du produit/articles"